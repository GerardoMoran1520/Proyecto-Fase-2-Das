@model Universidad.Models.Domain.Category
@{
}
<center>
    <form class="w-40" asp-action="Add" method="post">
        <h2 class="pb-10"><b>Agregar Tipo De Carrera</b></h2>
        <div class="input-container">
            <label asp-for="CategoryName" class="w-40"><b> Nombre De La Carrera</b></label>
            <input type="text" class="input" asp-for="CategoryName">
        </div>
        <div class="message-box pd-x-28 error">
            <span asp-validation-for="CategoryName"> </span>
        </div>

        @if (TempData["msg"] != null)
        {
            <div class="alert mb-1">
                @TempData["msg"]
            </div>
        }

        <br />
        <br />
        <center>
            <button class="btnn btn-defaultt" type="submit">Guardar</button>
            <a class="btnn btn-secc" href="/Category/CategoryList">Todos Los Tipos De Carrera</a>
        </center>
    </form>
</center>
